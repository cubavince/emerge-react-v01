//import './../styles/home.css';
import React, { useState, setEstate } from 'react';
const Options = (props) => {

    return (
        <div className="Start">
            Options:
        </div>
    );
}

export default Options;